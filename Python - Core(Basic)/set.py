# set: set is a collection of unique elements
#Note: set never follow the sequence of the list
# 1. it means set is not give the out according to Ascending and descending orders 'but' it will gives output in
# random numbers
# 2. Because the reason it set is follows the concepts of Hash and using Hash we can improve the performance and
# fetching elements as fast as possible
# 3. set is mutable - it means we can add, update, remove elements from the list

s = {22, 23, 26, 45, 34}
print(s)

# In 'set' we can't print same numbers at twice - it means duplication is not allowed

xx = {22, 44, 88, 33, 44, 55}
print(xx)

# in 'set' we can't change the value of the list because set is not support indexing
# Ex: print (s[2])

